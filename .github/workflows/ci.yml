## A basic GitHub Actions workflow for your Quarkus application.

name: CI build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  maven-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin
          cache: maven

      - name: Build
        run: ./mvnw -DskipTests package -B

  maven-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin
          cache: maven

      - name: Build
        run: ./mvnw test -B

  maven-it:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin
          cache: maven

      - name: Build
        run: ./mvnw verify -B

  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Docker Build
        run: docker build -f src/main/docker/Dockerfile.jvm -t ghcr.io/ffaarno/code-with-quarkus:${{ github.sha }} .

      - name: Docker Login
        run: echo ${{ secrets.ACTION_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Docker Push
        run: docker push ghcr.io/ffaarno/code-with-quarkus:${{ github.sha }}